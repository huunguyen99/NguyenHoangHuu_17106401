<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appnghenhac">

    <application
        android:allowBackup="true"
        android:icon="@drawable/appnghenhac"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appnghenhac"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppNgheNhac">
        <activity android:name=".PlayMusicActivity" android:theme="@style/Theme.AppNgheNhac.NoActionBar" >

        </activity>
        <activity
            android:name=".AlbumActivity"
            android:theme="@style/Theme.AppNgheNhac.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppNgheNhac.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".services.MyService"></service>
    </application>

</manifest>